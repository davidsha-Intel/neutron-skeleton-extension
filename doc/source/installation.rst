============
Installation
============

At the command line::

    $ pip install neutron-skeleton-extension

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv neutron-skeleton-extension
    $ pip install neutron-skeleton-extension
